services:
  api:
    image: api-exam:bartdewa/mlops-exam-backend
    build:
      context: api
      dockerfile: dockerfile
    environment:
      file_format: json
  frontend:
    image: frontend-exam:bartdewa/mlops-exam-frontend
    build:
      context: frontend
      dockerfile: frontend-dockerfile
    environment:
      API_HOST: api
      API_PORT: 8080
